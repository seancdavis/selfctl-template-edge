# 2026-08-21 — TanStack Start app with the kit mounted at /agent/* — run report

Spec: [`2026-08-21-tanstack-start-kit-mount.md`](./2026-08-21-tanstack-start-kit-mount.md).
Branch `13-tanstack-start-kit-mount`, issue #13. Run 2026-08-25, against
`@selfctl/agent-kit@0.4.0` / `@selfctl/protocol@0.2.0` packed from seancdavis/selfctl#22.

## Outcome

All 6 slices landed; every slice check run by the orchestrator before advancing; the
protocol smoke re-run by the orchestrator end to end. One Codex round each for security
(clean) and simplicity (three Low; two fixed, one kept on purpose). PR opened **draft** — the
lockfile step is Sean's.

## Slices

| # | Slice | Commit | Notes |
|---|---|---|---|
| 1 | Scaffold TanStack Start | `a701329` | Start 1.168.49, Netlify plugin 1.3.17, Vite 8.2.2. `src/routeTree.gen.ts` is generated by dev/build and gitignored — run one before `typecheck` on a fresh clone. |
| 2 | Mount the kit | `0b2d3e4`, `c3fdca7` | `selfctl.config.ts` keeps `id: "reference"`; `skills/notes.ts` is a pure move; 12 functions + `_shared/` deleted; CORS scoped to `/agent/*`. |
| 3 | Database | `e02699b` | Migration `20260825105826_woozy_doctor_octopus`: 8 `selfctl_*` tables created, 5 old tables dropped, singleton seeds appended. `drizzle-kit generate` needs a TTY (driven via `script`). The Vite plugin does **not** auto-apply migrations locally — `npx netlify database migrations apply` while dev runs. |
| 4 | Admin page | `fdc8444` | Key lives in component state only; `GET /agent/config/token` returns `{connectionToken}` (the spec said `{token}`). |
| 5 | Smoke | `8abe587` | `vite dev` runs the raw background + scheduled functions (`/_selfctl/turn` → 202, `/.netlify/functions/agent-tick`). Found: Netlify's config layer synthesizes `DEPLOY_PRIME_URL`/`URL` locally → the kit's self-call went to an unreachable host. Fixed in the kit (selfctl#22 `a1a2233`: `CONTEXT=dev` uses the request origin). `POST /agent/message` takes ~1.7 s locally because the emulator runs the background function synchronously; on Netlify it is a 202. |
| 6 | README + versions | `5798688`, `a63abd1` | README rewritten for the app shape; `package.json` pins `^0.4.0` / `^0.2.0`; lockfile still resolves the tarballs. |

## Orchestrator smoke (2026-08-25)

`GET /agent/summary` 401 → 200 (`protocolVersion "0.2"`, `kitVersion "0.4.0"`); `POST /agent/threads`;
`POST /agent/message` → `{turnId}` in 1.6 s; events `thread.created → turn.started → chat.appended → turn.finished`
with the Anthropic SDK's "Could not resolve authentication method" (no AI Gateway locally —
plumbing pass, not a dispatch 404); `POST /.netlify/functions/agent-tick` 200 twice, nothing
claimed the second time; no process left on 5173.

## Audit

- Security: no findings.
- Simplicity (Low ×3): `unlocked` state duplicated `settings` — fixed; unused `provider`
  fields in the page's types — fixed; `@selfctl/protocol` and `postgres` unused as direct
  deps — `postgres` removed, `@selfctl/protocol` **kept** (a fork rendering events needs the
  types; the spec pins it).

## Before merging (Sean)

1. Publish `@selfctl/protocol@0.2.0` and `@selfctl/agent-kit@0.4.0` from selfctl#22.
2. In this repo: `npm install` to regenerate `package-lock.json` against the published
   versions, commit, push — otherwise every Deploy-to-Netlify build fails.
3. Deploy preview: a real turn through AI Gateway; SSE through the CDN; the cron on a
   published deploy; the admin page in a browser; the desktop app with base URL
   `https://<site>/agent`.

## Kit follow-ups noticed (not this PR)

- `createAgentHandler` reads the config row before authorizing; a request with no bearer
  could 401 with zero DB work.
